# 📋 手动操作指南

## 请按以下步骤操作

---

## 第 1 步：打开 PowerShell 终端

1. 按 `Win + X` 键
2. 选择"Windows PowerShell"或"终端"
3. 或者直接按 `Win + R`，输入 `powershell`，回车

---

## 第 2 步：进入项目目录

在终端中输入：
```powershell
cd d:\ai学习\ai编程\衣物试穿
```

---

## 第 3 步：临时允许脚本执行（必须）

在终端中输入：
```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

---

## 第 4 步：安装依赖（首次运行）

在终端中输入：
```powershell
npm install
```

如果安装很慢，用国内镜像：
```powershell
npm install --registry=https://registry.npmmirror.com
```

**等待安装完成**，这可能需要几分钟。

---

## 第 5 步：启动开发服务器

在终端中输入：
```powershell
npm run dev
```

---

## 第 6 步：打开浏览器

看到类似这样的提示后：
```
- Local:        http://localhost:3000
```

打开浏览器访问：**http://localhost:3000**

---

## ✅ 完成！

---

## 💡 以后启动

以后只需要：
1. 打开终端
2. `cd d:\ai学习\ai编程\衣物试穿`
3. `npm run dev`

---

## ❓ 常见问题

**Q: 提示找不到 npm？**
重新打开终端，或者重启电脑。

**Q: npm install 卡住了？**
按 `Ctrl + C` 停止，然后重新运行。

**Q: 端口被占用？**
Next.js 会自动用其他端口（如 3001），注意看终端提示。
